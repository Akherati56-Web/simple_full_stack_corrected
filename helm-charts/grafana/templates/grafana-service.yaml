apiVersion: v1
kind: Service
metadata:
  name: grafana-ext
  namespace: monitoring
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      
  selector:
    app: grafana
    namespace: monitoring