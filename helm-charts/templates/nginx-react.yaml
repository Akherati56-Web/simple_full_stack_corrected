apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginxreact
  labels:
    app: nginxreact
spec:
  replicas: {{ .Values.replicas.nginx_react }}
  selector:
    matchLabels:
      app: nginxreact
  template:
    metadata:
      labels:
        app: nginxreact
    spec:
      containers:
        - name: nginxreact
          image: registry.gitlab.com/akherati5660/bookstore/nginx-react:latest
          ports:
            - containerPort: 80
          imagePullPolicy: Always
          resources:
            limits:
              memory: {{ .Values.memory.nginx_eact }}
              cpu: {{ .Values.cpu.nginx_react }}
      imagePullSecrets:
        - name: key
---
apiVersion: v1
kind: Service
metadata:
  name: nginxreact-service
spec:
  selector:
    app: nginxreact
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30002
