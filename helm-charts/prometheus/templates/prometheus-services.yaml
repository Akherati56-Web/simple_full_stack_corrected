apiVersion: v1
kind: Service
metadata:
 name: prometheus-server-ext
spec:
 type: NodePort
 ports:
    - protocol: TCP
      port: 80
      targetPort: 9090
      nodePort: 30090
 selector:
    app: prometheus-server