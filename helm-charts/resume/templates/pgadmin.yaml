apiVersion: apps/v1
kind: Deployment
metadata:
  name: pgadmin
spec:
  replicas: {{ .Values.replicas.pgadmin }}
  selector:
    matchLabels:
      app: pgadmin
  template:
    metadata:
      labels:
        app: pgadmin
    spec:
      containers:
        - name: pgadmin
          image: "{{ .Values.images.pgadmin }}:{{ .Values.tag.pgadmin }}"
          ports:
            - containerPort: 80
          imagePullPolicy: Always
          resources:
            limits:
              memory: {{ .Values.memory.pgadmin }}
              cpu: {{ .Values.cpu.pgadmin }}
      imagePullSecrets:
        - name: key
---
apiVersion: v1
kind: Service
metadata:
  name: pgadmin-service
spec:
  selector:
    app: pgadmin
  ports:
    - name: pgadmin
      port: 80
      targetPort: 80
