apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo_express
spec:
  replicas: {{ .Values.replicas.mongo_express }}
  selector:
    matchLabels:
      app: mongo_express
  template:
    metadata:
      labels:
        app: mongo_express
    spec:
      containers:
        - name: mongo_express
          image: "{{ .Values.images.mongo_express }}:{{ .Values.tag.mongo_express }}"
          ports:
            - containerPort: 8081
          imagePullPolicy: Always
          resources:
            limits:
              memory: {{ .Values.memory.mongo_express }}
              cpu: {{ .Values.cpu.mongo_express }}
      imagePullSecrets:
        - name: key
---
apiVersion: v1
kind: Service
metadata:
  name: mongo_express-service
spec:
  selector:
    app: mongo_express
  ports:
    - name: mongo_express
      port: 8081
      targetPort: 8081
